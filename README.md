# Zoekt webserver API nvim plugin
Use Zoekt webserver to search code, and thus jump to target code line

## Commands
ZoektSearch

this will get the search query and display the result

ZoektConfig

this only display current settings